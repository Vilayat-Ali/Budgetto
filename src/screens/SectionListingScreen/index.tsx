// lib
import React from 'react';
import {View, Text} from 'native-base';

type Props = {
  navigation: any;
};

const SectionListingScreen = ({navigation}: Props) => {
  return (
    <View>
      <Text>Section Listing Screen</Text>
    </View>
  );
};

export default SectionListingScreen;
